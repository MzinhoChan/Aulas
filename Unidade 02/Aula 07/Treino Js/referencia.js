const nome = "Maurício Boy";
const idade = 22;
const temCursoProgramacao = false;

const listas = ["Texto", 100, true, [], {}];
const usuario = {
    nome: "Mauricio",
    email: "email@email.com"
};

usuario.nome;
lista [0];

// Isso é uma função
function nomeDaFuncao () {}

// Isso é um loop
for(let i = 0; i < 10; i++) {}

// Isso é uma condicional
if (1 == 1){
}